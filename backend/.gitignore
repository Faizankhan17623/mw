node_modules
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.DS_Store
npm-debug.log*

package-lock.json

# Seeder / dev-only scripts
seeder.js

# Private planning files (root-level — handled by root .gitignore)
# ../BUGS_AND_ISSUES.md  ← ../ patterns don't work in git, removed
